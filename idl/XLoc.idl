#include </com/sun/star/uno/XInterface.idl>

module com { module loc { module crypto { module getinfo2 {

    interface XLoc2
    {
      // Modeled as an 'any' to allow it to be NULL/None/Missing.
      any cf1( [in] string a, [in] any b );
      // No parms, just get the whole market
      any cf2( );
      // Expecting parms to be exchange and function.
      any cf3( [in] string a, [in] any b, [in] any c );
    };

}; }; }; };
